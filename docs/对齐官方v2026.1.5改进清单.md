# Gopenclaw 对齐官方 OpenClaw v2026.1.5 (原名 clawdbot) 改进清单

> 基于官方 [OpenClaw v2026.1.5](https://github.com/openclaw/openclaw/releases/tag/v2026.1.5) 更新日志

## Highlights

### 1. Models
- [x] 添加 image-specific model config (`agent.imageModel` + fallbacks)
- [x] 添加 scan support (AgentConfig.ImageModel 字段)

### 2. Agent Tools
- [ ] 新增 `image` tool，路由到 image model (需要扩展 Agent)

### 3. Config
- [x] 默认 model shorthands (`opus`, `sonnet`, `gpt`, `gpt-mini`, `gemini`, `gemini-flash`)
- [x] AgentConfig.ModelShorts 支持
- [x] 环境变量覆盖支持

### 4. Docs
- [ ] 文档化 built-in model shorthands + precedence

## Fixes

### Control UI
- [ ] Render Markdown in tool result cards
- [ ] Prevent overlapping action buttons
- [ ] Chat view uses page scroll with sticky header/sidebar

### Android
- [ ] Tapping foreground service notification brings app to front

### Cron
- [x] Cron tool uses `id` for update/remove/run/runs

### macOS
- [ ] Treat location permission as always-only
- [ ] Make gateway protocol models `Sendable` for Swift 6
- [ ] Bundle QR code renderer modules

### WhatsApp
- [ ] Suppress typing indicator during heartbeat background tasks
- [ ] Mark offline history sync messages as read without auto-reply

### Discord
- [ ] Avoid duplicate replies when provider emits late streaming `text_end` events

### CLI
- [x] Use tailnet IP for local gateway calls when bind is tailnet/auto

### Env
- [x] Load global `$CLAWDBOT_STATE_DIR/.env` as fallback
- [x] Optional login-shell env fallback

### Agent Tools
- [ ] OpenAI-compatible tool JSON Schemas
- [x] Honor `agent.tools` allow/deny policy even when sandbox is off

### Onboarding
- [ ] Auto-build missing Control UI assets when running from source

### Discord/Slack
- [ ] Route reaction + system notifications to correct session

### Commands
- [ ] Unify /status and command auth across providers
- [ ] Group bypass for authorized control commands
- [ ] Remove Discord /clawd slash handler

---

## Gopenclaw 已实现

- ✅ Gateway WebSocket 协议
- ✅ Agent LLM 调用
- ✅ Cron 调度器
- ✅ Webhook 处理
- ✅ 通道适配器 (Telegram/Discord/Slack/WhatsApp/Feishu)
- ✅ TTS 语音合成
- ✅ Skills/插件系统
- ✅ Browser CDP 控制
- ✅ Model shorthands 配置
- ✅ Image model 配置
- ✅ 环境变量覆盖

## 完整复现要求

本项目遵循"不要简化"原则，以上所有条目均需完整实现，不得简化或跳过任何功能。
