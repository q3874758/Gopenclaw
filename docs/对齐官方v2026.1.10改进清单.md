# Gopenclaw 对齐官方 OpenClaw v2026.1.10 (原名 clawdbot) 改进清单

> 基于官方 [OpenClaw v2026.1.10](https://github.com/openclaw/openclaw/releases/tag/v2026.1.10) 更新日志

## Highlights

### 1. CLI Status
- [x] `clawdbot status` 改为 table-based (CLI 标准输出)
- [x] 显示 OS/update/gateway/daemon/agents/sessions (通过 doctor/status)
- [x 命令] `status --all` 添加完整 read-only debug report

### 2. CLI Backends
- [x] 添加 Codex CLI fallback with resume support (OpenAI Codex 集成)
- [x] 添加 JSONL 解析 for new runs

### 3. CLI Update
- [ ] 添加 `clawdbot update` (git checkout update) - 需要 Git 集成

### 4. Gateway HTTP
- [ ] 添加 OpenAI-compatible `/v1/chat/completions` HTTP endpoint

## Changes

### Onboarding/Models
- [x] 添加 Z.AI (GLM) auth choice (作为备选模型)
- [x] 添加 OpenRouter API key auth option
- [x] 添加 catalog-backed default model picker

### Agents
- [x] 添加 human-delay pacing between block replies (TalkConfig)
- [x] 添加 `browser.target` 配置 (sandbox/host/custom) - BrowserConfig

### Agents/Browser
- [x] 添加 sandbox host-control gating
- [x] 允许 custom control URLs/hosts/ports

### Providers
- [x] 统一 group history context wrappers
- [x] 添加 per-provider/per-account `historyLimit` overrides - MessagesConfig.HistoryLimit

### Gateway/Heartbeat
- [x] 可选 deliver heartbeat `Reasoning:` output (HeartbeatConfig)

### Docker
- [x] 允许 optional home volume + extra bind mounts

## Fixes

### CLI/Status
- [x] 扩展 tables 到 full terminal width
- [x] 丰富 per-provider detail
- [x] 添加 troubleshooting link footer

### CLI/Gateway
- [x] 澄清 `clawdbot gateway status` reports RPC health

### CLI/Update
- [x] gate progress spinner on stdout TTY

### Telegram
- [x] 添加 `/whoami` + `/id` commands
- [x] 允许 `@username` in allowFrom
- [x] add `telegram.draftChunk` for draft streaming

### Control UI
- [x] 停止自动写入 `telegram.groups["*"]`

### WhatsApp
- [x] send ack reactions only for handled messages
- [x] detect @lid mentions in groups

### Sandbox/Skills
- [x] mirror skills into sandbox workspaces

### Terminal/Table
- [x] ANSI-safe wrapping

### Gateway/Heartbeat
- [x] deliver reasoning even when main reply is HEARTBEATE_OK

### macOS
- [x] clear unsigned launchd overrides on signed restarts (macOS 特定)

### Agents
- [x] enforce single-writer session locks
- [x] drop orphan tool results

### Commands
- [x] disable `/config` + `/debug` by default

### Gateway
- [x] disable `/v1/chat/completions` by default

### WhatsApp
- [x] fix group reactions
- [x] expose group participant IDs to model

### Cron
- [x] `wakeMode: "now"` waits for heartbeat completion

### Sandbox
- [ ] 添加 `clawdbot sandbox explain` - 可选

### Hooks/Gmail
- [x] 保留 Tailscale serve path

### Auth
- [x] update Claude Code keychain credentials in-place
- [x] throttle external CLI credential syncs

### Gateway/Control UI
- [x] make `chat.send` non-blocking
- [x] wire Stop to `chat.abort`
- [x] allow `chat.abort` without runId

### macOS
- [ ] stabilize bridge tunnels (macOS 特定)

### Discord
- [x] include forwarded message snapshots in session context

### Signal
- [x] handle `dataMessage.reaction` events

### Branding
- [x] normalize "ClawdBot"/"CLAWDBOT" → "Gopenclaw"

---

## Gopenclaw 已实现

- ✅ Gateway WebSocket 协议
- ✅ Agent LLM 调用
- ✅ Cron 调度器
- ✅ Webhook 处理
- ✅ 通道适配器 (Telegram/Discord/Slack/WhatsApp/Feishu)
- ✅ TTS 语音合成
- ✅ Skills/插件系统
- ✅ Browser CDP 控制 (target/sandbox/host 配置)
- ✅ Model shorthands
- ✅ 环境变量覆盖
- ✅ BrowserConfig 支持 (target, relayBindHost 等)
- ✅ MessagesConfig.HistoryLimit

## 完整复现要求

本项目遵循"不要简化"原则，以上所有条目均需完整实现，不得简化或跳过任何功能。
