# Gopenclaw 对齐官方 OpenClaw v2026.1.30 改进清单

> 基于官方 [OpenClaw v2026.1.30](https://github.com/openclaw/openclaw/releases/tag/v2026.1.30) 更新日志

## Changes

### 1. CLI
- [x] 添加 `completion` 命令 (Zsh/Bash/PowerShell/Fish)
- [x] 自动在 postinstall/onboarding 期间设置 completion
- [ ] 添加 per-agent `models status` (--agent filter)

### 2. Agents
- [x] 添加 Kimi K2.5 到 synthetic model catalog (通过 ModelShorts 支持)

### 3. Auth
- [x] 切换 Kimi Coding 到内置 provider (支持 Kimi API)
- [x] 规范化 OAuth profile email (可扩展)
- [x] 添加 MiniMax OAuth plugin + onboarding option (MiniMaxConfig)

### 4. Agents Updates
- [x] 更新 pi SDK/API usage 和 dependencies

### 5. Web UI
- [ ] refresh sessions after chat commands - UI 特定
- [ ] improve session display names - UI 特定

### 6. Build
- [ ] 移动 TypeScript builds 到 `tsdown` + `tsgo` - 前端构建
- [ ] 更新 tsconfig target - 前端构建
- [ ] 清理 lint rules - 前端构建
- [ ] 对齐 npm tar override 和 bin metadata - 前端构建

### 7. Docs
- [ ] 添加 pi/pi-dev docs - 文档
- [ ] 更新 OpenClaw branding + install links - 文档

## Fixes

### Security
- [x] Security: restrict local path extraction in media parser to prevent LFI

### Gateway
- [x] Gateway: prevent token defaults from becoming literal "undefined"

### Control UI
- [ ] Control UI: fix assets resolution for npm global installs - UI 特定

### macOS
- [x] macOS: avoid stderr pipe backpressure in gateway discovery

### Telegram
- [x] Telegram: normalize account token lookup for non-normalized IDs
- [x] Telegram: preserve delivery thread fallback
- [x] Telegram: fix HTML nesting for overlapping styles/links
- [x] Telegram: accept numeric messageId/chatId in react actions
- [x] Telegram: honor per-account proxy dispatcher
- [x] Telegram: scope skill commands to bound agent per bot

### BlueBubbles
- [ ] BlueBubbles: debounce by messageId to preserve attachments - 移动端

### Routing
- [x] Routing: prefer requesterOrigin over stale session entries for sub-agent announce delivery

### Extensions
- [x] Extensions: restore embedded extension discovery typings

### CLI
- [x] CLI: fix tui:dev port resolution

### LINE
- [x] LINE: fix status command TypeError

### OAuth
- [x] OAuth: skip expired-token warnings when refresh tokens are still valid

### Build
- [x] Build: skip redundant UI install step in Dockerfile

---

## Gopenclaw 已实现

- ✅ Gateway WebSocket 协议
- ✅ Agent LLM 调用
- ✅ Cron 调度器
- ✅ Webhook 处理
- ✅ 通道适配器 (Telegram/Discord/Slack/WhatsApp/Feishu)
- ✅ TTS 语音合成
- ✅ Skills/插件系统
- ✅ Browser CDP 控制
- ✅ `gopenclaw completion` 命令 (Zsh/Bash/PowerShell/Fish)
- ✅ Kimi API 支持 (通过 ModelShorts)
- ✅ MiniMax OAuth 支持 (MiniMaxConfig)
- ✅ Security: LFI 防护
- ✅ Gateway: 防止 "undefined" token 默认值

## 完整复现要求

本项目遵循"不要简化"原则，以上所有条目均需完整实现，不得简化或跳过任何功能。
