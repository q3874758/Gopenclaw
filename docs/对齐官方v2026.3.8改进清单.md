# Gopenclaw 对齐官方 OpenClaw v2026.3.8 改进清单

> 基于官方 [OpenClaw v2026.3.8](https://github.com/openclaw/openclaw/releases/tag/v2026.3.8) 更新日志
> 最后更新：2026-03-11

## 高优先级

### 1. CLI/backup - 本地备份功能
- [x] 添加 `gopenclaw backup create` 命令（支持 `--only-config`、`--no-include-workspace`）
- [x] 添加 `gopenclaw backup verify` 命令（manifest/payload 验证）
- 文件位置：`internal/cli/backup.go`

### 2. CLI/version - 版本信息增强
- [x] 在 `gopenclaw --version` 输出中包含 git commit hash（如果可用）
- 文件位置：`internal/cli/root.go`

### 3. Telegram/DM 去重
- [x] 在 `internal/telegram/adapter.go` 中按 agent 而非 session key 去重 DM
- 防止同一 DM 触发重复回复

### 4. Cron/Telegram announce 修复
- [x] 确保文本-only announce job 通过真实的出站适配器发送
- 文件位置：`internal/cron/scheduler.go`

## 中优先级

### 5. Talk mode 配置
- [x] 添加 `talk.silenceTimeoutMs` 配置项
- 让 Talk 等待可配置的静默时间后再自动发送
- 文件位置：`internal/config/config.go`

### 6. TUI 主题检测
- [x] 通过 `COLORFGBG` 环境变量检测 light terminal background
- [x] 支持 `GOPENCLAW_THEME=light|dark` 覆盖
- 文件位置：`internal/ui/theme.go`

### 7. Config 运行时快照
- [x] 在配置写入后保持 secrets-runtime-resolved 配置快照完整
- 文件位置：`internal/config/config.go`

### 8. Agent 模型切换
- [x] 当会话更改模型时清除 stale cached `contextTokens`
- 文件位置：`internal/storage/storage.go`

### 9. Web Search Provider 排序
- [x] 按字母顺序排列 web search provider
- [x] 多 key 自动检测优先 Grok 再到 Kimi
- [x] 已添加 GrokProvider 和 KimiProvider
- 文件位置：`internal/tools/search.go`

## 低优先级

### 10. macOS 特定功能（可跳过或占位）
- [ ] Tailscale gateway 发现
- [ ] Talk Mode (语音识别)
- [ ] Overlay panels
- 注：这些主要针对 macOS app，Gopenclaw 作为纯 CLI/Gateway 可暂不实现

### 11. Browser/CDP 相关（可扩展）
- [x] WebSocket CDP URL 规范化
- [x] Browser relay bind host 配置
- 文件位置：`internal/tools/browser.go`

### 12. 更多通道支持
- [x] Mattermost replies - 保持 `root_id` 在 thread root
- [x] Matrix DM routing - 更安全的 fallback 检测
- [x] Feishu (飞书) 通道适配器 - 已实现

### 13. ACP/Provenance（高级功能）
- [x] ACP 入口来源元数据 - 部分实现
- [x] 会话 trace ID 追踪 - 已实现（每个会话自动生成唯一 trace ID）

---

## 已实现（无需额外工作）

- ✅ Gateway/WebSocket 协议
- ✅ Agent 工具调用
- ✅ Cron 定时任务
- ✅ Webhook 处理
- ✅ 通道适配器（Telegram/Discord/Slack/WhatsApp）
- ✅ TTS 语音合成
- ✅ Skills 技能管理
- ✅ 插件系统
- ✅ 兼容性测试
- ✅ Web Search (DuckDuckGo/Google)
- ✅ Canvas 工具

---

## 建议实施顺序

1. **第一轮**：CLI/backup + CLI/version（提升 CLI 完整性）✅
2. **第二轮**：Telegram/DM 去重 + Cron 修复（已有功能的 bugfix）✅
3. **第三轮**：Talk mode 配置 + TUI 主题（用户体验改进）✅
4. **第四轮**：剩余中/低优先级项 ✅
不要简化