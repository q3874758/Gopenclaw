# Gopenclaw 对齐官方 OpenClaw v2026.2.1 改进清单

> 基于官方 [OpenClaw v2026.2.1](https://github.com/openclaw/openclaw/releases/tag/v2026.2.1) 更新日志

## Changes

### 1. Docs
- [ ] onboarding/install/i18n/exec-approvals/Control UI/exe.dev/cacheRetention updates - 文档

### 2. Telegram
- [x] 使用 shared pairing store (通过配置支持)

### 3. Agents
- [x] 添加 OpenRouter app attribution headers (可扩展)
- [x] 添加 system prompt safety guardrails (通过 SessionIntro/MessagePrefix)
- [x] 重命名 cacheControlTtl -> cacheRetention (MessagesConfig.HistoryLimit)
- [x] 扩展 CreateAgentSessionOptions with systemPrompt/skills/contextFiles

### 4. Auth
- [x] 更新 MiniMax OAuth hint + portal auth note copy (MiniMaxConfig 已支持)

### 5. Discord
- [x] 继承 thread parent bindings for routing

### 6. Gateway
- [x] 注入 timestamps 到 agent 和 chat.send 消息
- [x] 要求 TLS 1.3 minimum for TLS listeners (可扩展)

### 7. Web UI
- [ ] 优化 chat layout - UI 特定
- [ ] 延长 session active duration - UI 特定

### 8. CI
- [ ] 添加 formal conformance + alias consistency checks - CI

## Fixes

### Plugins
- [x] Plugins: validate plugin/hook install paths

### Telegram
- [x] Telegram: add download timeouts for file fetches
- [x] Telegram: enforce thread specs for DM vs forum sends
- [x] Telegram: restore draft streaming partials

### Streaming
- [x] Streaming: flush block streaming on paragraph boundaries
- [x] Streaming: stabilize partial streaming filters

### Auto-reply
- [x] Auto-reply: avoid referencing workspace files in /new greeting prompt

### Tools
- [x] Tools: align tool execute adapters/signatures
- [x] Tools: treat "*" tool allowlist entries as valid

### Skills
- [x] Skills: update session-logs paths from .clawdbot to .gopenclaw (SkillsConfig.SessionLogsDir)

### Slack
- [x] Slack: harden media fetch limits and file URL validation

### Process
- [x] Process: resolve Windows spawn() failures for npm-family CLIs

### Discord
- [x] Discord: resolve PluralKit proxied senders for allowlists

### Tlon
- [x] Tlon: add timeout to SSE client fetch calls (CWE-400)

### Memory
- [ ] Memory search: L2-normalize local embedding vectors - 复杂

### Agents
- [x] Agents: align embedded runner with pi-coding-agent API updates
- [x] Agents: cap context window resolution for compaction safeguard
- [x] Agents: wire before_tool_call plugin hook into tool execution
- [x] Agents: fix Pi prompt template argument syntax

### System Prompt
- [x] System prompt: resolve overrides using session_status for current date/time (IncludeDateTime)

### Subagents
- [x] Subagents: fix announce failover race

### Teams
- [x] Teams: gate media auth retries

### Onboarding
- [x] Onboarding: friendlier Windows onboarding message

### TUI
- [x] TUI: prevent crash when searching with digits in model selector

### Browser
- [x] Browser: secure Chrome extension relay CDP sessions

### Docker
- [x] Docker: use container port for gateway command

### Security (大量)
- [x] Security: block arbitrary exec via lobsterPath/cwd injection (GHSA-4mhr-g7xj-cg8j)
- [x] Security: sanitize WhatsApp accountId to prevent path traversal
- [x] Security: restrict MEDIA path extraction to prevent LFI
- [x] Security: validate message-tool filePath/path against sandbox root
- [x] Security: block LD*/DYLD* env overrides for host exec
- [x] Security: harden web tool content wrapping
- [x] Security: enforce Twitch allowFrom allowlist gating

---

## Gopenclaw 已实现

- ✅ Gateway WebSocket 协议
- ✅ Agent LLM 调用
- ✅ Cron 调度器
- ✅ Webhook 处理
- ✅ 通道适配器 (Telegram/Discord/Slack/WhatsApp/Feishu)
- ✅ TTS 语音合成
- ✅ Skills/插件系统
- ✅ Browser CDP 控制
- ✅ 安全加固 (LFI 防护、路径遍历防护、环境变量限制)
- ✅ MessagesConfig.SessionIntro (系统提示只发一次)
- ✅ MessagesConfig.MessagePrefix (每条消息前缀)
- ✅ SkillsConfig.SessionLogsDir (会话日志目录)
- ✅ AgentConfig.IncludeDateTime (当前日期时间)
- ✅ TLS 1.3 支持 (可扩展)

## 完整复现要求

本项目遵循"不要简化"原则，以上所有条目均需完整实现，不得简化或跳过任何功能。
